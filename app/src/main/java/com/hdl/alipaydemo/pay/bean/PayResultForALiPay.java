package com.hdl.alipaydemo.pay.bean;import android.text.TextUtils;import com.google.gson.Gson;import java.util.Map;/** * 支付宝支付结果 */public class PayResultForALiPay {    private String resultStatus;//支付状态码    private ALiPayOrderResult result;//结果集    private String memo;//操作信息    public PayResultForALiPay(Map<String, String> rawResult) {        if (rawResult == null) {            return;        }        for (String key : rawResult.keySet()) {            if (TextUtils.equals(key, "resultStatus")) {                resultStatus = rawResult.get(key);            } else if (TextUtils.equals(key, "result")) {                result = new Gson().fromJson(rawResult.get(key), ALiPayOrderResult.class);            } else if (TextUtils.equals(key, "memo")) {                memo = rawResult.get(key);            }        }    }    @Override    public String toString() {        return "resultStatus={ " + resultStatus + " };memo={ " + memo                + " };result={ " + result + " }";    }    /**     * @return the resultStatus     */    public String getResultStatus() {        return resultStatus;    }    /**     * @return the memo     */    public String getMemo() {        return memo;    }    /**     * @return the result     */    public ALiPayOrderResult getResult() {        return result;    }}